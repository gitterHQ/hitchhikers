<section class="settings-container">
  <form class="settings-container__form" action="#">

    <h1 class="settings-container__form__title">
      How much should the guide know about you?
    </h1>

    <div class="settings-container__form__location"  data-component="user-location-input"></div>
    <h2 class="settings-container__form__label">
      Show people where you're from and how far you've travelled
    </h2>

    <div data-component="user-email-input"></div>
    <h2 class="settings-container__form__label">
      Receive one email before the event with awesome people to meet
    </h2>

    {{#if distance}}
      <button class="setting-container__form__submit">
        Save
      </button>
    {{else}}
      <button class="setting-container__form__submit">
        Find other travellers
      </button>
    {{/if}}
  </form>

  <p class="settings-container__detail">
    This is just a fun little hack from the team at
    <a class="settings-container__detail__link" href="http://gitter.im"> Gitter </a> to help people meet each other at universe. All data is temporarily stored.
  </p>

  <form class="hidden" data-component="details">
    <input type="text" {{#if lat}}     value="{{lat}}"    {{/if}} name="lat" />
    <input type="text" {{#if lon}}     value="{{lon}}"    {{/if}} name="lng" />
    <input type="text" {{#if code}}    value="{{code}}"   {{/if}} name="country_short" />
    <input type="text" {{#if city}}    value="{{city}}"   {{/if}} name="locality" />
    <input type="text" {{#if country}} value="{{country}}"{{/if}} name="country" />
  </form>
</section>
